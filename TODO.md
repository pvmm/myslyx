# high priority (one commit each)
- [x] firefox `multiedit/readonly-startup` is flaky: the first suite run after the 6s boot settle (runner.py:90), relies on fixed wait_for_timeout(400) typing settles. Harden it with poll-based waits and re-verify on firefox + chromium.
- [x] if changing the LANG of a file creates a name clash with another file, ask the user to rename the filename until the new name no longer clashes.
- [x] add button to download all files in the file pool ("DOWNLOAD\nALL") as a zipped single file. Put it left of "RESET FILE POOL", unstacked.
- [x] the sub folding in HitBasic displays "sub...end sub" when folded, but I would like it to display the name of the subroutine too, like the way the folding in the C language displays the name of the function even when the function is folded. The same thing happens to "function...end function" in HitBasic. Can you fix it to display the name of the function/sub even when folded?
- [x] the if/end if folding in HitBasic displays "if...end if" when folded, but I would like it to display the test condition of the condition too, like the way the folding of conditions work in the C language. Can you fix it?
- [x] when user presses Ctrl+, to display the favicon menu, the top item of the menu should be activated instead of no item being activated, forcing the user to move the arrow keys to figure out where the cursor is.
- [x] mark code with sleep antipattern in the "Sensitive / flaky code" list below.
- [x] create an example of a link in the HINTS documentation. For instance: the for keyword in the C language root page is now a link pointing to the "FOR Loop" hint. Make the link appear like a dashed line below the text of the link.
- [x] add slider to HINTS panel to adjust font size. Save it to Myslyx config so you can restore the setting back.

# Sensitive / flaky code to keep an eye on (check each when fixed):
- [x] `window.__wbPyBridge` (editor_page.py:76-91) = unguarded global storage API; Python writes whole file pool via json.dumps into run_javascript (375-379). Any JS can call setFiles/setActive and bypass _open_file_request, dedupe, rename/conflict checks and the readonly STARTUP flag. (Now normalized at boot adoption: _normalize_storage_pool drops non-dict/dupe entries, coerces shapes, maps unknown langs to Text, re-locks STARTUP.txt.)
- [x] Active-editor singletons `__wbEditorId`/`__wbEditorIds` (495, 577, 718, 927-934) + getElement(...).editorPromise races; helpers/plugins read these synchronously. Root cause of the old "Language not found" boot race and typing-before-mount drops. (Now centralized in window.WBEditorActive (retro.js): current() polls the CURRENT __wbEditorId until the view mounts (up to 3s) and only yields it if it is still the active id at resolution; withView() wraps a callback. Adopted by editor-keyboard.js wbCmDo (fixes the no-null-guard boot Ctrl+Z crash), plugins.js, hints.js, folding.js, and the test readers helpers.py doc_text/doc_unchanged_for/doc_equals.)
- [x] Settings rows (WRAP/LIGATURES) (1022-1200): fire-and-forget saveConfig + inline restyle, no ack; tests add fixed 400ms waits; querySelectorAll('.cm-editor .cm-content') restyles all editors. (The restyle itself is synchronous, so the WRAP/LIGATURES rows now stamp a machine-readable data-value ack synchronously; helpers.wait_for_settings_value polls it. open_settings and the wrap/ligatures tests now poll instead of sleeping; tests/helpers.py:47-56 sleeps removed.)
- [x] _load_file_into_editor (541-660): f-string values interpolated into run_javascript ({readonly}, {cm_lang}); a quote in any value breaks the JS; readonly STARTUP enforced client-side only. (Now every value interpolated into run_javascript is emitted as a JSON literal via json.dumps — file ids, names, languages, hint keys, editor ids, attribute lookups, event detail, font names. Test smoke/js-interpolation-quoting loads a pool with a quote-laden id/name and asserts the values reach the page intact.)
- [x] _on_editor_change trusts client value; tests read doc via view editorPromise (helpers.py:6-9) not the Python echo, so content is only ever validated client-side. (Content is now sanitized server-side (_sanitize_content: non-str dropped, capped at 2MiB) at every ingestion point — pool adoption, _open_file_request, on_change. New smoke/server-echoes-edits compares the server-echoed pool against the live CM view on both typing and undo, proving the round trip instead of trusting either side alone.)
- [x] Sleep-antipattern hotbeds (69 fixed waits in 7 files): type/click → `wait_for_timeout` → assert, where the target state is observable and should be polled with `wait_for_function` instead. Locations: helpers.py:47-56,81 (open_settings/set_language settles); test_smoke.py (18 sites, e.g. 9/91-95/314-431 type-then-400ms); test_header.py (28,42,56,78-105); test_plugins.py (132-228, reload-resume waits 800-2500ms); test_multiedit.py (38-96 typing settles, 111-148 undo-group separators 300-800ms — this group needs a real settle by design); infrastructure boot settles runner.py:90 (6000ms) and debug_suite.py:158,171 (5000ms). Prefer poll-based waits (h.doc_equals, h.doc_unchanged_for, wait_for_function) wherever the awaited change is observable. (helpers.set_language/go now poll the menu open/close and the active dock tab instead of sleeping; test_smoke/test_header/test_multiedit assert via h.doc_equals/doc_unchanged_for/wait_for_function; test_plugins.py gained _wait_render (snapshot-consistent, deadline-corrected), wait_for_selector for reload resumes, per-suite plugin-config reset (config survives ?reset=1, so suites were order-dependent: disable-via-config leaked user-swatches:false into lazy-disabled-by-default), and a bounded retry for the flaky prog.c drop-import. retro.js WBEditorActive.current() is now deadline-bounded on every path (a wedged plugin import used to hang it forever). Remaining fixed waits are by design: negative/observation windows, the undo-group separator, and infra boot settles. Full suite green 43/43; plugins group green 9/9.)
